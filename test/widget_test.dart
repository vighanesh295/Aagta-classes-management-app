// Widget tests for Aagte Classes app.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder test', (WidgetTester tester) async {
    // Placeholder — Firebase cannot be initialised in test environment.
    expect(true, isTrue);
  });
}
